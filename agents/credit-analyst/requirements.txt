# bedrock-agentcore is pinned <1.2 by strands-agents-tools 0.8.5; this agent
# uses identity.auth (stable across both lines) + the CI wrapper.
bedrock-agentcore>=1.1.0,<1.2.0
strands-agents==1.48.0
strands-agents-tools[agent_core_code_interpreter]==0.8.5
mcp>=1.19.0
boto3>=1.40.0
uvicorn>=0.30.0
aws-opentelemetry-distro>=0.12.1
